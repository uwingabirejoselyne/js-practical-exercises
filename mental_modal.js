// a function called combineNames that can take any number of names as strings and return a single string of all names combined.

const combineNames =(...names)=>{
    // concatnate the arguments
    // let result = []
    // names.forEach((args)=>{
    //     args.slice(0,lat)
    // })
    let =


    return names.slice(-1).toUpperCase().join(' ')
    
}
console.log(combineNames('David'));
console.log(combineNames('John', 'Doe') );
console.log(combineNames('Jean', 'marie', 'Vianney'));